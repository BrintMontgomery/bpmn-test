{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://oklahoma.gov/odmhsas/ofc/bpmn/ir-schema-v1.json",
  "title": "OFC BPMN intermediate representation",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "process_id",
    "participant_name",
    "process_doc",
    "lanes",
    "phases",
    "nodes",
    "edges"
  ],
  "properties": {
    "schema_version": {"const": 1},
    "process_id": {"type": "string", "minLength": 1},
    "participant_name": {"type": "string", "minLength": 1},
    "process_doc": {"type": "string"},
    "process_name": {"type": "string", "minLength": 1},
    "participant_id": {"type": "string", "minLength": 1},
    "collaboration_id": {"type": "string", "minLength": 1},
    "definitions_id": {"type": "string", "minLength": 1},
    "exporter": {"type": "string", "minLength": 1},
    "exporter_version": {"type": "string", "minLength": 1},
    "target_namespace": {"type": "string", "minLength": 1},
    "ann_above": {
      "type": "array",
      "description": "Optional lane IDs whose annotation bands render above their lanes. Do not use source filenames, version text, lane names, or prose.",
      "items": {"type": "string", "minLength": 1},
      "uniqueItems": true
    },
    "lane_classes": {
      "type": "object",
      "additionalProperties": {"type": "string", "minLength": 1}
    },
    "mermaid_class_defs": {
      "type": "array",
      "items": {"type": "string"}
    },
    "lanes": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "#/$defs/lane"}
    },
    "phases": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "#/$defs/phase"}
    },
    "nodes": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "#/$defs/node"}
    },
    "edges": {
      "type": "array",
      "items": {"$ref": "#/$defs/edge"}
    },
    "external_pools": {
      "type": "array",
      "items": {"$ref": "#/$defs/external_pool"}
    },
    "message_flows": {
      "type": "array",
      "items": {"$ref": "#/$defs/message_flow"}
    },
    "documents": {
      "type": "array",
      "description": "Optional manifest of generated BPMN output documents. IDs are process IDs and files must be generated .bpmn filenames; source Markdown files do not belong here.",
      "items": {"$ref": "#/$defs/document"}
    },
    "decomposition": {
      "$ref": "#/$defs/decomposition"
    }
  },
  "$defs": {
    "lane": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name"],
      "properties": {
        "id": {"type": "string", "minLength": 1},
        "name": {"type": "string"},
        "ann_above": {"type": "boolean"}
      }
    },
    "phase": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name"],
      "properties": {
        "id": {"type": "string", "minLength": 1},
        "name": {"type": "string"}
      }
    },
    "node": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "kind", "lane", "phase", "name"],
      "properties": {
        "id": {"type": "string", "minLength": 1},
        "kind": {
          "enum": [
            "task",
            "gateway_x",
            "gateway_p",
            "start_message",
            "catch_timer",
            "catch_message",
            "end",
            "subprocess",
            "call_activity",
            "link_throw",
            "link_catch"
          ]
        },
        "lane": {"type": "string", "minLength": 1},
        "phase": {"type": "string", "minLength": 1},
        "name": {"type": "string"},
        "ttype": {"enum": ["manual", "user", "send", "receive"]},
        "doc": {
          "type": "array",
          "items": {"type": "string"}
        },
        "note": {"type": ["string", "null"]},
        "parent": {"type": ["string", "null"], "minLength": 1},
        "collapsed": {"type": "boolean"},
        "called_element": {"type": "string", "minLength": 1},
        "link_name": {"type": "string", "minLength": 1}
      },
      "oneOf": [
        {
          "properties": {"kind": {"const": "task"}}
        },
        {
          "properties": {
            "kind": {
              "enum": [
                "gateway_x",
                "gateway_p",
                "start_message",
                "catch_timer",
                "catch_message",
                "end",
                "subprocess",
                "call_activity",
                "link_throw",
                "link_catch"
              ]
            }
          },
          "not": {"required": ["ttype"]}
        }
      ]
    },
    "edge": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "target"],
      "properties": {
        "source": {"type": "string", "minLength": 1},
        "target": {"type": "string", "minLength": 1},
        "label": {"type": "string"},
        "condition": {"type": "string", "description": "Branch condition. Omit or leave empty for the single default branch of an exclusive gateway."},
        "loop": {"type": "boolean"}
      }
    },
    "external_pool": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "anchor"],
      "properties": {
        "id": {"type": "string", "minLength": 1},
        "name": {"type": "string"},
        "anchor": {"type": "string", "minLength": 1},
        "width": {"type": "number", "exclusiveMinimum": 0},
        "height": {"type": "number", "exclusiveMinimum": 0},
        "gap_above": {"type": "number", "minimum": 0},
        "mermaid_id": {"type": ["string", "null"], "minLength": 1}
      }
    },
    "message_flow": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "target"],
      "properties": {
        "id": {"type": "string", "minLength": 1},
        "source": {"type": "string", "minLength": 1},
        "target": {"type": "string", "minLength": 1},
        "name": {"type": "string"},
        "mermaid_name": {"type": ["string", "null"]},
        "label_width": {"type": "number", "exclusiveMinimum": 0},
        "label_height": {"type": "number", "exclusiveMinimum": 0},
        "label_dx": {"type": "number"},
        "label_dy": {"type": "number"}
      }
    },
    "document": {
      "type": "object",
      "description": "A generated BPMN output document in a process bundle, not a source SOP file.",
      "additionalProperties": false,
      "required": ["id", "file"],
      "properties": {
        "id": {"type": "string", "minLength": 1},
        "file": {"type": "string", "minLength": 1},
        "role": {"enum": ["main", "global"]}
      }
    },
    "decomposition": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "mode": {"enum": ["off", "auto"]},
        "max_nodes_per_plane": {"type": "integer", "minimum": 1},
        "max_columns": {"type": "integer", "minimum": 1},
        "max_pool_width": {"type": "number", "exclusiveMinimum": 0},
        "collapse_phases": {
          "type": "array",
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1}
        },
        "max_lanes_per_collapsed_phase": {
          "type": "integer",
          "minimum": 1
        }
      }
    }
  }
}
